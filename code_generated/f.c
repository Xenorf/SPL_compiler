#include <stdio.h>
#include <stdlib.h>
int main()
{
    int integer, i, a;
    float d, e;
    e = 450;
    scanf(" %d", &integer);
    if (integer <= 5 || integer >= 12)
    {
        printf("%d", integer);
    }
    printf("%d", 35);
    printf("\n");
    register int _by1;
    for (integer = 1; _by1 = 5, (integer - (15)) * ((_by1 > 0) - (_by1 < 0)) <= 0; integer += _by1)
    {
        printf("%d", integer);
        printf("\n");
    }
    a = 0;
    while (a < 10)
    {
        printf("%d", a);
        a = a + 1;
    }
    return 0;
}
